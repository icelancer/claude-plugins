[project]
name = "common"
version = "0.1.0"
description = "Python shared common library"
readme = "README.md"
requires-python = ">={{PYTHON_VERSION}}"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
